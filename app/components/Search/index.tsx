import { Input } from '@mui/material';
import "./index.scss";
const Search = () => {
	return (
		<div className="search-wrap">
      <Input autoFocus={false} />
		</div>
	);
};
export default Search;
